# О проекте
Утилита реализована для получения MAC-адреса цели по её IPv4 адресу. В качестве единственного аргумента исполняемый файл принимает IPv4 адрес из **локальной** сети. Пример использования:

```c
$ gcc ./arp.c
$ sudo a.out 192.168.0.244
```
